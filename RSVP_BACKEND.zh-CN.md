# 零基础宾客管理后台教程

> 目标：让朋友填写的姓名、是否参加、人数、忌口和留言真正汇总到一个后台，并能导出 Excel。整个邀请函只改 `config.js` 一行，不需要购买服务器，也不要删除原有登记表。

## 一、先理解“演示模式”和“真实后台”

项目默认是安全的演示模式：登记内容只保存在填写者自己的浏览器中，寿星看不到名单。这样适合公开展示，但不适合正式统计。

需要正式使用时，最省心的办法是使用在线表单平台。表单平台负责保存和导出数据，邀请函只负责打开填写页面。

## 二、选择表单平台

可使用金数据、腾讯问卷、WPS 表单或其他你信任的平台。界面、免费额度和导出权限可能调整，请以平台当前说明为准。本教程以“金数据”为例，因为创建、发布和导出流程比较直观。

- 金数据：<https://jinshuju.net/>
- [金数据官方发布说明](https://help.jinshuju.net/v4/articles/wxform)
- [金数据官方导出说明](https://help.jinshuju.net/v4/articles/dataexport/)

## 三、创建生日赴约登记表

1. 登录表单平台，新建空白表单，标题可写“我的生日派对赴约登记”。
2. 按下面顺序添加字段：

| 字段 | 推荐类型 | 是否必填 |
| --- | --- | --- |
| 姓名 | 单行文字 | 是 |
| 是否参加 | 单选：参加 / 遗憾缺席 | 是 |
| 同行人数 | 数字或下拉选择 | 参加时填写 |
| 忌口或过敏 | 单行或多行文字 | 选填 |
| 联系方式 | 手机或单行文字 | 选填 |
| 给寿星的留言 | 多行文字 | 选填 |

3. 在表单说明中写明数据用途，例如：“仅用于统计生日聚会人数、餐食和联络安排，活动结束后删除。”
4. 发布表单，用自己的手机提交一条测试记录。
5. 回到表单后台，确认能看到这条记录。

## 四、复制正确的公开填写链接

在表单的“发布/分享”页面复制朋友填写链接。它通常以 `https://` 开头。

不要复制浏览器地址栏里的后台编辑链接，也不要把账号、密码、API 密钥或宾客名单放进项目文件。

## 五、邀请函只改一行

1. 用文本编辑器打开 `config.js`。
2. 在文件末尾找到：

```js
rsvpUrl: '',
```

3. 把刚才的公开填写链接粘贴到两个单引号中：

```js
rsvpUrl: 'https://你的公开表单链接',
```

4. 保存文件并刷新邀请函。登记区会自动变成“填写赴约信息”按钮。

请勿删除 `index.html` 里的登记表，也不要修改 `app.js`。以后把链接改回空字符串 `''`，即可恢复本地演示模式。

## 六、查看、筛选和导出名单

1. 登录你创建表单时使用的账号。
2. 打开对应表单的“数据/回收/结果”页面。
3. 按“是否参加”等字段筛选，汇总总人数和忌口。
4. 使用平台的导出功能下载 Excel 或 CSV。
5. 活动结束后，先保留一份确有需要的备份，再关闭收集并按承诺删除不再需要的数据。

## 七、正式分享前必须测试

- [ ] 使用手机打开部署后的 HTTPS 邀请函，而不是电脑本地文件。
- [ ] 登记区显示“填写赴约信息”，且按钮能打开正确表单。
- [ ] 用一条虚拟信息完成提交，后台能看到记录。
- [ ] 必填项、参加条件和成功提示正常。
- [ ] 能导出测试记录，并删除这条测试数据。
- [ ] 不同手机重复打开表单时不会看到其他人的信息。

## 八、常见问题

### 点击后仍显示演示表单

确认链接粘贴在 `config.js` 的 `rsvpUrl` 两个引号之间，并且已重新上传修改后的 `config.js`。若网页仍是旧版本，在网址后加 `?v=2` 再打开。

### 打开的是平台后台或要求朋友登录

复制错了链接。回到“发布/分享”页面，复制“公开填写链接”，并检查表单是否允许目标访客填写。

### 朋友提交了，但后台没有记录

先确认打开的是同一张表单的数据后台，再用无痕窗口亲自提交一条测试记录。默认演示表单的数据只在填写者手机里，不会进入后台。

### 想把第三方表单直接嵌进邀请函

不建议新手这样做。部分平台禁止被网页嵌入，微信内也可能出现登录、滚动或白屏问题。外部按钮方式更稳定；只有平台明确提供官方嵌入代码，并且已在微信和手机浏览器测试通过时再使用。

## 九、隐私提醒

只收集活动确实需要的信息，限制后台查看人员，开启平台提供的访问保护。电话、忌口等属于个人信息，不要把导出的名单上传到公开 GitHub 仓库，也不要在群聊中传播。
