# 完全零基础定制教程

这份教程写给不会代码的新手。你只需要准备信息、修改一个文件、替换可选素材，就能完成自己的生日邀请函。

## 一、先准备这些内容

- 寿星姓名或昵称
- 生日聚会日期与开始时间
- 聚会地点、交通说明、地图分享链接
- 当天流程，例如签到、晚餐、蛋糕、游戏
- 一句邀请文案和一句礼物说明
- 可选：如果不使用模板中的官方音乐试听，准备一首有权公开使用的音乐

## 二、只修改 config.js

用记事本、VS Code 或其他文本编辑器打开 `config.js`。修改引号中的文字，保留英文引号、冒号和每行末尾的英文逗号。

例如：

```js
heroName: '小马',
dateTime: '2027-10-01T18:30:00+08:00',
dateLabel: '2027年10月1日 · 星期五',
timeLabel: '18:30',
venue: '上海市某某餐厅 · 二层包间',
mapUrl: '你的地图分享链接',
```

`dateTime` 专门用于倒计时，格式必须是：

```text
年-月-日T小时:分钟:秒+08:00
```

国内使用通常保留结尾的 `+08:00`。

## 三、修改流程

每一组大括号是一条流程：

```js
{ time: '18:30', title: '海面集合', detail: '签到 · 合影', stars: '★' },
```

可以修改文字，也可以复制整行增加流程。删除时要删除从 `{` 到 `},` 的完整一行。

## 四、设置背景音乐

模板默认使用与婚礼请柬版一致的《潜水员戴夫》官方音乐远程试听地址，不需要下载音乐文件。访客点击“接受委托”或右下角音乐按钮后播放。

手机浏览器通常禁止网页自动播放，这是正常现象，无法用代码强制绕过。请在邀请朋友时提醒对方点一下页面上的音乐按钮。

如需换成自己的音乐：

1. 准备一首有权公开使用、体积较小的 MP3。
2. 把它放入 `assets` 文件夹，例如 `birthday-bgm.mp3`。
3. 在 `config.js` 最后填写：

```js
musicUrl: './assets/birthday-bgm.mp3',
```

`musicUrl` 保持空字符串 `''` 时会改用模板内置的原创像素芯片音乐。希望完全关闭音乐按钮时，把：

```js
musicEnabled: true,
```

改为 `false`。

官方试听音频没有收录在项目里，远程地址将来可能变化或失效。长期公开传播或商业使用前，建议换成原创音乐或已经获得相应用途授权的音频。

## 五、预览

双击 `index.html`。重点检查：

- 首页姓名、时间和地点是否正确
- 长按“接受委托”是否成功
- “生日补给打捞”能否开始、计分、扣氧气和显示通关彩蛋
- 角色来电能否切换
- 日程是否完整
- 地图按钮是否打开正确链接
- 登记表能否保存和修改

建议在手机上再打开一次，或在浏览器开发者工具里检查 375px、390px、430px 三种宽度。

## 六、登记功能说明

默认登记只保存在访客自己的设备中。它适合展示交互，不适合真正统计名单。

> 💡 **零基础最快方案**：用腾讯问卷 / 金数据建一张赴约登记表，把请柬登记区替换成表单入口，寿星在表单后台直接看名单、导出 Excel。完整步骤（含可直接复制的替换代码）见 [后台宾客管理接入教程](./RSVP_BACKEND.zh-CN.md)。

需要真实收集时，也可以接入：

- 合规的在线表单
- 自己的服务器和数据库
- Cloudflare Workers + D1
- Supabase 或 Firebase

上线前至少要说明收集目的、限制后台访问人员、使用 HTTPS，并避免收集与聚会无关的信息。

## 七、发布到 GitHub Pages

### 第一次上传

1. 登录 [GitHub](https://github.com/)，点击右上角 `+`，选择 **New repository**。
2. 仓库名建议填写 `dave-diver-birthday-invitation`，可见范围选择 **Public**。
3. 创建仓库后，点击 **uploading an existing file**。
4. 打开本项目文件夹，把里面的全部文件和 `assets` 文件夹拖到上传区域。不要只上传最外层文件夹。
5. 页面底部填写更新说明，例如“我的生日邀请函”，点击 **Commit changes**。
6. 进入仓库的 **Settings → Pages**。
7. Source 选择 **Deploy from a branch**，Branch 选择 `main`，Folder 选择 `/ (root)`，点击 **Save**。

通常等待 2～10 分钟后，正式地址是：

```text
https://你的GitHub用户名.github.io/仓库名/
```

本模板的在线地址是：

```text
https://mastrena.github.io/dave-diver-birthday-invitation/
```

如果出现 404，先等几分钟，再回到 **Settings → Pages** 检查分支是否为 `main`、目录是否为 `/ (root)`，并确认仓库根目录里能直接看到 `index.html`。

## 八、把邀请函分享给亲朋好友

### 1. 分享前先自测

1. 复制 GitHub Pages 的正式网址，不要复制浏览器里以 `file:///` 开头的本地地址，也不要复制 GitHub 仓库代码地址。
2. 用浏览器无痕窗口打开正式网址，确认陌生访客不登录也能访问。
3. 用手机检查首页、音乐、长按委托、补给打捞、角色来电、地图和登记表。
4. 点击地图按钮，确认打开的是正确地点。
5. 注意：默认登记只保存在访客自己的手机中，寿星无法在后台集中看到结果。

### 2. 微信好友或群聊分享

直接把完整的 `https://...github.io/.../` 链接粘贴给好友或群聊。可以配一段简单说明，例如：

```text
我的生日任务已发布！点开邀请函后，记得开启音乐并完成补给打捞，期待和你见面 🎂
```

微信可能会缓存旧标题、旧图片或旧页面。更新邀请函后，可在正式链接后加一个版本参数再分享，例如：

```text
https://你的GitHub用户名.github.io/仓库名/?v=2
```

以后再次更新可以改成 `?v=3`、`?v=4`。这不会改变网页内容，只是帮助浏览器重新获取新版本。

### 3. 生成二维码

在电脑浏览器打开正式网址，使用浏览器菜单中的“分享”或“创建二维码”功能；也可以使用你信任的二维码工具。生成后先用自己的手机扫码测试，再把二维码发给亲友或放进海报。

二维码中只放公开网页链接，不要把宾客名单、手机号、密码或其他私人资料上传到二维码网站。

### 4. 修改后再次发布

以后只需修改 `config.js`，再把更新后的文件上传到同一个 GitHub 仓库并提交。等待几分钟，原来的正式网址会自动更新，不需要重新创建仓库，也不用重新启用 Pages。

## 九、常见问题

### 打开后没有音乐

先点击“接受委托”或右下角音乐按钮。若仍无声音，检查手机是否静音、网络是否正常；远程官方试听地址如果失效，可按第四节换成本地 MP3。

### 页面只有文字，没有图片或样式

确认 `index.html`、`style.css`、`app.js`、`config.js` 和 `assets` 都上传到了仓库根目录，文件名大小写没有被修改。

### 朋友填了登记，但我看不到

这是模板的安全演示模式：数据只存在对方自己的设备。需要真正统计人数时，应接入合规在线表单或后端。

### 修改后还是旧内容

等待 GitHub Pages 完成更新，然后刷新页面，或在链接末尾添加新的 `?v=数字` 再打开。

## 十、发布前检查清单

- [ ] 姓名、日期、星期和时间正确
- [ ] 地点和地图链接正确
- [ ] 流程完整且没有示例文字
- [ ] 点击音乐按钮可以播放；如果替换音乐，确认拥有公开使用权限
- [ ] 手机页面没有文字溢出或按钮遮挡
- [ ] 已确认登记是演示模式，或真实后端已经测试
- [ ] 公开仓库中没有私人电话、住址、密码或密钥
- [ ] 正式 GitHub Pages 链接已在无痕窗口和手机中测试
- [ ] 分享的是 `github.io` 正式链接，不是本地地址或 GitHub 代码仓库地址
- [ ] 已阅读版权风险说明
