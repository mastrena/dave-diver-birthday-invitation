# 2026-08-30

## 项目首次部署到 GitHub

- 目标：把本地静态项目（潜水员戴夫风格生日邀请函）部署为 GitHub 新仓库并开启在线访问。
- 结果：已创建公开仓库 https://github.com/mastrena/dave-diver-birthday-invitation（默认分支 main），推送 21 个文件（含 assets 下 9 张图片，共 5.2MB），启用 GitHub Pages（源：main 分支根目录），线上地址 https://mastrena.github.io/dave-diver-birthday-invitation/ ，构建状态 built，首页/CSS/JS/图片均返回 200。
- 仓库描述指向"下潜 → 生日委托 → 20 秒补给打捞 → 日程地图 → 赴约登记"的交互流程；homepage 字段已设为 Pages 地址。

## 关键约束（踩坑记录）

1. GitHub MCP 连接器**无创建仓库权限**：`mcp__github__create_repository` 返回 403 `Resource not accessible by integration`，只能改走 git/REST。
2. 二进制素材无法通过 MCP 上传：`push_files` / `create_or_update_file` 的 content 为纯文本，传图片会导致编码损坏，必须 git push。
3. 本机环境无 `gh` CLI、无 `~/.gitconfig`、无钥匙串凭据；commit 时用 `git -c user.name=... -c user.email=...` 内联指定作者（mastrena / mastrena@users.noreply.github.com）。
4. Fine-grained PAT 无法创建仓库，创建仓库需要 classic token 的 `repo` scope（用完即弃，不落盘；push 用 URL 内联 token，不写 remote，因此 .git 无凭证残留——已 grep 验证）。

## 内容安全

- config.js 全为占位内容（"寿星名字"/"示例市"），无真实姓名、地址、联系方式，公开无隐私风险。
- 风险仍在：assets 内 9 张《潜水员戴夫》游戏素材随 Public 仓库公开可下载，COPYRIGHT_NOTICE.md 已声明非官方粉丝创作，用户确认接受该模式（其婚礼请柬仓库同为 Public）。

## README 重写（对齐婚礼版体例）

- 参照 mastrena/dave-diver-wedding-invitation 的 README 结构重写生日版 README.md，已提交（commit 08e4fa4）并推送，线上已生效。
- 结构：标题 → 在线 Demo → 页面包含什么 → 数据不收集说明 → 三分钟开始 → AI 提示词（与 AI_SETUP_PROMPT.md 一致）→ 与婚礼版差异对照表 → 文件说明 → Pages 发布 → 分享方法 → 版权声明 → 许可。
- 事实均从代码核对得出，非臆测：深度 HUD 随滚动 0→420M、氧气条 scaleX(1-进度×0.22)；小游戏 20 秒 / 蛋糕+3·礼物+2·气球+1·垃圾扣 O₂ / 集满 8 件通关；最高分与登记表均存 localStorage（key: blue-hole-birthday-supply-best）；支持 prefers-reduced-motion 与 IntersectionObserver 揭示动画；音乐需用户点击触发。
- 未添加婚礼版的赞赏码章节（生日版 assets 无 sponsor 图片）。
- 本地 Bash 的 grep 对 app.js 返回空（原因未明），改用 Grep 工具（ripgrep）正常；curl 拉 raw 文件在沙箱内被 SIGTERM，改用 GitHub MCP get_file_contents 验证远端内容。
- git push 首次报 "Error in the HTTP2 framing layer"，用 `git -c http.version=HTTP/1.1 push` 成功——后续推送优先用该参数。
